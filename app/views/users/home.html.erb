<div class="compliance">
  <h3><%= "Compliance Sets needing Review" unless @compliances_user.empty? %></h3>
  <table class="compliance_sets">
    <%= render :partial => 'compliances/compliance_summary', :collection => @compliances_user, :as => :compliance   unless @compliances_user.empty? %>
  </table>
</div>